#include <bits/stdc++.h>
using namespace std;

//iterate over the deltas
//store the targets hit from the end position and the start position in reverse order, leading to the computation of the max for the delat
// this is within a counter array finding the points in which the targets are hit (we need to maintain this array in order to account)
// for the duplicate hits
//store max variable

int pref[i];
