#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;  cin >> n;
    int k; cin >> k;
    vector<int> v;
    int arr[n+2];
    for(int i = 0 ; i < k; i++){
        int a; cin >> a;
    
        arr[a] = 0;
    }
    int sol[n+2];
    //iterate over the delta
    
}