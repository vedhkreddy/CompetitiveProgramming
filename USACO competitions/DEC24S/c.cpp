#include <bits/stdc++.h>
using namespace std;

int main(){
    /*
        probably want to remove the trees that satisfy the most intervals as possible
        first, find the 
    */
    int t; cin >> t;
    while (t--){
        
    }
}